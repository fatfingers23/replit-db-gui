import store from '@/store';

const baseUrl = import.meta.env.VITE_BASE_NODE_URL;

// const options = {
//   headers: new Headers({'db_url': store.dbUrl})
// };
//
//
//
// async function getKeys(prefix = ''): Array<string>{
//   const result = fetch();
// }
